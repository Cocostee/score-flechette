# Mode Équipes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un jeu en équipe (2v2, 3v3, 2v2v2 + custom) à tous les modes, avec score partagé et lancers alternés, sans casser le jeu individuel.

**Architecture:** Modèle « côtés » (side) — en solo `sideId === playerId` (rétro-compatible), en équipe `sideId === teamId`. Le score partagé (`states`), les manches (`legsWon`) et le vainqueur sont indexés par `sideId` ; les stats restent par joueur. Un `order` (liste de playerIds interleavée) pilote la rotation des tours. Les fonctions de scoring existantes sont réutilisées en leur passant l'état du côté.

**Tech Stack:** Next.js 16, React 19, TypeScript strict, CSS Modules. Client-only reducer (`useReducer`), persistance localStorage.

## Global Constraints

- TypeScript strict — pas de `any` implicite ; toute valeur typée.
- PAS de runner de tests dans le repo. Vérification de chaque tâche = `npx tsc --noEmit` (ZÉRO sortie) et, pour les tâches UI, `npm run build` (« Compiled successfully »). Ne pas inventer de tests unitaires.
- **Non-régression solo impérative** : en mode individuel, `teams = null`, `sideOf` est l'identité, `order = players.map(p => p.id)` → comportement strictement identique à l'existant.
- Cap : 2–3 équipes, ≤ 6 joueurs au total, chaque équipe ≥ 1 joueur.
- `GameMode` = `"x01" | "cricket" | "cutthroat" | "aroundclock"`.
- Messages de commit terminés par `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure

| Fichier | Statut | Responsabilité |
|---|---|---|
| `src/utils/teams.ts` | nouveau | `buildOrder`, `buildSideOf`, `setupSides`, `sidesAsPlayers`, couleurs/labels |
| `src/interfaces/index.ts` | modifié | `Team` + champs `teams`/`sideOf`/`order` (GameState), `teams?` (GameConfig) |
| `src/hooks/useDartsGame.ts` | modifié | reducer indexé par côté + HYDRATE rétro-compat |
| `src/utils/ranking.ts` | modifié | classement par côté |
| `src/utils/gameSummary.ts` | modifié | une ligne par joueur avec place/manches d'équipe |
| `src/components/screens/SetupScreen.tsx` (+ `.module.css`) | modifié | toggle Individuel/Équipes, presets, cartes éditables |
| `src/components/ui/TeamScoreCard.tsx` (+ `.module.css`) | nouveau | carte d'équipe (score partagé + lignes joueurs) |
| `src/components/screens/GameScreen.tsx` (+ `.module.css`) | modifié | branche solo/équipe, lanceur courant |
| `src/components/screens/ResultScreen.tsx` (+ `.module.css`) | modifié | podium par équipe |

---

## Task 1: Moteur — modèle « côtés »

**Files:**
- Create: `src/utils/teams.ts`
- Modify: `src/interfaces/index.ts`
- Modify: `src/hooks/useDartsGame.ts`

**Interfaces:**
- Produces:
  - `interface Team { id: string; name: string; playerIds: string[]; color: string }`
  - GameState gagne `teams: Team[] | null`, `sideOf: Record<string,string>`, `order: string[]`.
  - GameConfig gagne `teams?: Team[]`.
  - `src/utils/teams.ts` exporte `buildOrder(teams): string[]`, `buildSideOf(teams): Record<string,string>`, `setupSides(players, teams): SideSetup`, `sidesAsPlayers(teams, players): Player[]`, `teamLabel(i): string`, `teamColor(i): string`, `TEAM_COLORS`.
  - `interface SideSetup { teams: Team[] | null; sideIds: string[]; sideOf: Record<string,string>; order: string[] }`

- [ ] **Step 1: Créer `src/utils/teams.ts`**

```ts
import type { Player, Team } from "@/interfaces";

export const TEAM_COLORS = ["teamA", "teamB", "teamC"] as const;

const TEAM_LETTERS = ["A", "B", "C"];

/* Default display label for the nth team. */
export function teamLabel(index: number): string {
  return `Équipe ${TEAM_LETTERS[index] ?? index + 1}`;
}

/* Accent-colour key for the nth team. */
export function teamColor(index: number): string {
  return TEAM_COLORS[index % TEAM_COLORS.length];
}

/* Builds the interleaved throwing order (playerIds) across teams:
   A1,B1,A2,B2,... Smaller teams cycle their members. */
export function buildOrder(teams: Team[]): string[] {
  const nonEmpty = teams.filter((t) => t.playerIds.length > 0);
  if (nonEmpty.length === 0) {
    return [];
  }
  const maxLen = Math.max(...nonEmpty.map((t) => t.playerIds.length));
  const order: string[] = [];
  for (let i = 0; i < maxLen; i += 1) {
    for (const t of nonEmpty) {
      order.push(t.playerIds[i % t.playerIds.length]);
    }
  }
  return order;
}

/* Maps each playerId to its side (team) id. */
export function buildSideOf(teams: Team[]): Record<string, string> {
  const map: Record<string, string> = {};
  for (const t of teams) {
    for (const pid of t.playerIds) {
      map[pid] = t.id;
    }
  }
  return map;
}

export interface SideSetup {
  teams: Team[] | null;
  sideIds: string[];
  sideOf: Record<string, string>;
  order: string[];
}

/* Derives the runtime side layout from teams, or players in solo mode. */
export function setupSides(
  players: Player[],
  teams: Team[] | undefined | null,
): SideSetup {
  if (teams && teams.length > 0) {
    return {
      teams,
      sideIds: teams.map((t) => t.id),
      sideOf: buildSideOf(teams),
      order: buildOrder(teams),
    };
  }
  const sideOf: Record<string, string> = {};
  for (const p of players) {
    sideOf[p.id] = p.id;
  }
  return {
    teams: null,
    sideIds: players.map((p) => p.id),
    sideOf,
    order: players.map((p) => p.id),
  };
}

/* Returns the scoring sides as Player-shaped entities (for cricket helpers
   that iterate participants by id/name). */
export function sidesAsPlayers(
  teams: Team[] | null,
  players: Player[],
): Player[] {
  if (teams) {
    return teams.map((t) => ({ id: t.id, name: t.name }));
  }
  return players;
}
```

- [ ] **Step 2: Étendre `src/interfaces/index.ts`**

Add the `Team` interface right after the `Player` interface (after its closing brace):

```ts
export interface Team {
  id: string;
  name: string;
  playerIds: string[];
  color: string;
}
```

In `interface GameState`, add three fields right after the `players: Player[];` line:

```ts
  teams: Team[] | null;
  sideOf: Record<string, string>;
  order: string[];
```

In `interface GameConfig`, add after `players: Player[];`:

```ts
  teams?: Team[];
```

- [ ] **Step 3: Reducer — imports et helpers de construction**

In `src/hooks/useDartsGame.ts`, add to the type import block from `@/interfaces` the `Team` and `X01Rules` types (they are not yet imported). The import list becomes:

```ts
import type {
  AroundClockPlayerState,
  CricketPlayerState,
  DartThrow,
  GameConfig,
  GameMode,
  GameState,
  Multiplier,
  Player,
  PlayerGameState,
  PlayerStats,
  Team,
  X01PlayerState,
  X01Rules,
} from "@/interfaces";
```

Add a new import for the teams helpers after the aroundClock import block:

```ts
import { setupSides, sidesAsPlayers } from "@/utils/teams";
```

- [ ] **Step 4: Reducer — `buildStates` et `buildLegs` par côté**

Replace the whole `buildStates` function (currently takes `config: GameConfig`) with a side-keyed version:

```ts
/* Builds the initial scoring states, one per side. */
function buildStates(
  mode: GameMode,
  rules: X01Rules,
  sideIds: string[],
): Record<string, PlayerGameState> {
  const states: Record<string, PlayerGameState> = {};
  for (const sideId of sideIds) {
    states[sideId] =
      mode === "x01"
        ? createX01State(rules)
        : mode === "aroundclock"
          ? createAroundClockState()
          : createCricketState();
  }
  return states;
}
```

Replace `buildLegs` (currently takes `players: Player[]`) with a side-keyed version:

```ts
/* Builds a zeroed legs-won tally for every side. */
function buildLegs(sideIds: string[]): Record<string, number> {
  const legs: Record<string, number> = {};
  for (const sideId of sideIds) {
    legs[sideId] = 0;
  }
  return legs;
}
```

- [ ] **Step 5: Reducer — `replayTurn` opère sur un côté**

Replace the `replayTurn` function so its `currentId` parameter is the SIDE id and its `players` parameter is the sides-as-players list. Only the doc comment and parameter semantics change; the body already keys by the passed id:

```ts
/* Replays the darts of the current turn from a snapshot to rebuild the
   active side's state. `currentSideId` keys the scoring state; `sides` is the
   participant list cricket needs (teams in team mode, players in solo). */
function replayTurn(
  snapshot: Record<string, PlayerGameState>,
  currentSideId: string,
  sides: Player[],
  mode: GameMode,
  rules: GameState["rules"],
  darts: DartThrow[],
): Record<string, PlayerGameState> {
  if (mode === "x01") {
    const states = cloneStates(snapshot);
    let current = snapshot[currentSideId] as X01PlayerState;
    for (const dart of darts) {
      current = applyX01Dart(current, dart, rules).state;
    }
    states[currentSideId] = current;
    return states;
  }

  if (mode === "aroundclock") {
    const states = cloneStates(snapshot);
    let current = snapshot[currentSideId] as AroundClockPlayerState;
    for (const dart of darts) {
      current = applyAroundClockDart(current, dart).state;
    }
    states[currentSideId] = current;
    return states;
  }

  let map = cloneStates(snapshot) as Record<string, CricketPlayerState>;
  const cutThroat = mode === "cutthroat";
  for (const dart of darts) {
    map = applyCricketDart(map, currentSideId, sides, dart, cutThroat);
  }
  return map;
}
```

- [ ] **Step 6: Reducer — `reduceRegister` par côté**

Replace the whole `reduceRegister` function with this side-aware version. Key differences from the current code: the current player comes from `state.order`, the scoring key is the side, stats stay keyed by player, cricket uses `sidesAsPlayers`.

```ts
/* Resolves a registered dart for the active player and returns the next state. */
function reduceRegister(state: GameState, dart: DartThrow): GameState {
  if (state.turnOver || state.winnerId || state.darts.length >= 3) {
    return state;
  }

  const currentId = state.order[state.currentIndex];
  const sid = state.sideOf[currentId] ?? currentId;
  const darts = [...state.darts, dart];
  const prior = state.stats[currentId];

  // ── Around the Clock ──────────────────────────────────────────────────────
  if (state.mode === "aroundclock") {
    const current = state.states[sid] as AroundClockPlayerState;
    const result = applyAroundClockDart(current, dart);
    const atcStats: PlayerStats = {
      ...prior,
      darts: prior.darts + 1,
      marks: prior.marks + (result.advanced ? 1 : 0),
    };
    return {
      ...state,
      states: { ...state.states, [sid]: result.state },
      darts,
      turnOver: result.win || darts.length >= 3,
      winnerId: result.win ? sid : null,
      stats: { ...state.stats, [currentId]: atcStats },
      legsWon: result.win
        ? { ...state.legsWon, [sid]: state.legsWon[sid] + 1 }
        : state.legsWon,
    };
  }

  // ── Shared base stats (x01 + cricket) ────────────────────────────────────
  const baseStats: PlayerStats = {
    darts: prior.darts + 1,
    bestVisit: prior.bestVisit,
    lastVisit: prior.lastVisit,
    marks: prior.marks + dartMarks(dart),
    tonPlus: prior.tonPlus,
    oneEighties: prior.oneEighties,
    checkoutAttempts: prior.checkoutAttempts,
    checkoutHits: prior.checkoutHits,
    pointsScored: prior.pointsScored,
  };

  // ── x01 ──────────────────────────────────────────────────────────────────
  if (state.mode === "x01") {
    const current = state.states[sid] as X01PlayerState;
    const result = applyX01Dart(current, dart, state.rules);

    if (result.bust) {
      return {
        ...state,
        states: cloneStates(state.turnSnapshot ?? state.states),
        darts,
        turnOver: true,
        bust: true,
        stats: { ...state.stats, [currentId]: baseStats },
      };
    }

    let finalStats = { ...state.stats, [currentId]: baseStats };

    if (result.win) {
      const snapState = state.turnSnapshot?.[sid];
      const isCheckoutChance =
        snapState?.kind === "x01" &&
        snapState.opened &&
        snapState.score > 0 &&
        snapState.score <= 170;
      const turnPointsTotal = darts.reduce((s, d) => s + d.points, 0);
      finalStats = {
        ...state.stats,
        [currentId]: {
          ...baseStats,
          pointsScored: prior.pointsScored + turnPointsTotal,
          checkoutAttempts: baseStats.checkoutAttempts + (isCheckoutChance ? 1 : 0),
          checkoutHits: baseStats.checkoutHits + (isCheckoutChance ? 1 : 0),
        },
      };
    }

    return {
      ...state,
      states: { ...state.states, [sid]: result.state },
      darts,
      turnOver: result.win || darts.length >= 3,
      winnerId: result.win ? sid : null,
      stats: finalStats,
      legsWon: result.win
        ? { ...state.legsWon, [sid]: state.legsWon[sid] + 1 }
        : state.legsWon,
    };
  }

  // ── Cricket / Cut-throat ──────────────────────────────────────────────────
  const cutThroat = state.mode === "cutthroat";
  const sides = sidesAsPlayers(state.teams, state.players);
  const states = applyCricketDart(
    state.states as Record<string, CricketPlayerState>,
    sid,
    sides,
    dart,
    cutThroat,
  );
  const win = checkCricketWin(states, sid, sides, cutThroat);

  return {
    ...state,
    states,
    darts,
    turnOver: win || darts.length >= 3,
    winnerId: win ? sid : null,
    stats: { ...state.stats, [currentId]: baseStats },
    legsWon: win
      ? { ...state.legsWon, [sid]: state.legsWon[sid] + 1 }
      : state.legsWon,
  };
}
```

- [ ] **Step 7: Reducer — `DEFAULT_STATE` gagne les champs côté**

In `DEFAULT_STATE`, add after `players: [],`:

```ts
  teams: null,
  sideOf: {},
  order: [],
```

- [ ] **Step 8: Reducer — `START_GAME`, `NEXT_LEG`, `NEW_GAME` construisent les côtés**

Replace the `START_GAME` case body with:

```ts
    case "START_GAME": {
      const sides = setupSides(action.config.players, action.config.teams);
      const states = buildStates(
        action.config.mode,
        action.config.rules,
        sides.sideIds,
      );
      return {
        screen: "game",
        mode: action.config.mode,
        rules: action.config.rules,
        players: action.config.players,
        teams: sides.teams,
        sideOf: sides.sideOf,
        order: sides.order,
        states,
        currentIndex: 0,
        darts: [],
        turnSnapshot: cloneStates(states),
        turnOver: false,
        bust: false,
        winnerId: null,
        round: 1,
        stats: buildStats(action.config.players),
        totalStats: buildStats(action.config.players),
        legsTarget: action.config.legsTarget,
        legsWon: buildLegs(sides.sideIds),
        startIndex: 0,
        recorded: false,
        past: [],
      };
    }
```

Replace the `NEXT_LEG` case body with (rebuilds side states, rotates start by one throwing slot, keeps legsWon):

```ts
    case "NEXT_LEG": {
      const newTotalStats = accumulateAllStats(state);
      const sides = setupSides(state.players, state.teams);
      const startIndex = (state.startIndex + 1) % state.order.length;
      const states = buildStates(state.mode, state.rules, sides.sideIds);
      return {
        ...state,
        states,
        currentIndex: startIndex,
        startIndex,
        darts: [],
        turnSnapshot: cloneStates(states),
        turnOver: false,
        bust: false,
        winnerId: null,
        round: 1,
        stats: buildStats(state.players),
        totalStats: newTotalStats,
        past: [],
      };
    }
```

Replace the `NEW_GAME` case body with:

```ts
    case "NEW_GAME": {
      const sides = setupSides(state.players, state.teams);
      const states = buildStates(state.mode, state.rules, sides.sideIds);
      return {
        ...state,
        screen: "game",
        states,
        currentIndex: 0,
        darts: [],
        turnSnapshot: cloneStates(states),
        turnOver: false,
        bust: false,
        winnerId: null,
        round: 1,
        stats: buildStats(state.players),
        totalStats: buildStats(state.players),
        legsWon: buildLegs(sides.sideIds),
        startIndex: 0,
        recorded: false,
        past: [],
      };
    }
```

- [ ] **Step 9: Reducer — `REMOVE_FROM` et `NEXT_TURN` par côté**

In the `REMOVE_FROM` case, replace the lines that compute `currentId` and call `replayTurn` (currently using `state.players[state.currentIndex].id` and `state.players`) with:

```ts
      const darts = state.darts.slice(0, action.index);
      const currentId = state.order[state.currentIndex];
      const sid = state.sideOf[currentId] ?? currentId;
      const states = replayTurn(
        state.turnSnapshot,
        sid,
        sidesAsPlayers(state.teams, state.players),
        state.mode,
        state.rules,
        darts,
      );
```

In the `NEXT_TURN` case, replace the `endingId` line and the ATC snapshot lookups and the `currentIndex` line. Specifically:

Replace `const endingId = state.players[state.currentIndex].id;` with:

```ts
      const endingId = state.order[state.currentIndex];
      const endSide = state.sideOf[endingId] ?? endingId;
```

Inside the `visit` IIFE, in the `aroundclock` branch, replace the two lookups `state.turnSnapshot?.[endingId]` and `state.states[endingId]` with `state.turnSnapshot?.[endSide]` and `state.states[endSide]` respectively.

Replace the later `const snapState = state.turnSnapshot?.[endingId];` (used for the x01 checkout-attempt check) with:

```ts
      const snapState = state.turnSnapshot?.[endSide];
```

Replace `const currentIndex = (state.currentIndex + 1) % state.players.length;` with:

```ts
      const currentIndex = (state.currentIndex + 1) % state.order.length;
```

(The `stats` object stays keyed by `endingId` — per player. No other change in this case.)

- [ ] **Step 10: Reducer — `HYDRATE` rétro-compat**

In the `HYDRATE` case return object, add three defaulted fields (after `round: saved.round ?? 1,`):

```ts
        teams: saved.teams ?? null,
        sideOf:
          saved.sideOf ??
          Object.fromEntries(saved.players.map((p) => [p.id, p.id])),
        order: saved.order ?? saved.players.map((p) => p.id),
```

- [ ] **Step 11: Hook — `currentPlayer` / `currentState` via l'ordre et le côté**

In the `useMemo` at the bottom of `useDartsGame`, replace the `currentPlayer` / `currentState` computation with:

```ts
    const currentId =
      state.order.length > 0 ? state.order[state.currentIndex] : null;
    const currentPlayer = currentId
      ? state.players.find((p) => p.id === currentId) ?? null
      : null;
    const currentState = currentPlayer
      ? state.states[state.sideOf[currentPlayer.id] ?? currentPlayer.id] ?? null
      : null;
```

- [ ] **Step 12: Vérifier typage + build (solo doit rester intact)**

Run: `npx tsc --noEmit && npm run build`
Expected: zéro sortie tsc ; « Compiled successfully ». (Le mode équipe n'est pas encore atteignable depuis l'UI ; le solo doit fonctionner exactement comme avant.)

- [ ] **Step 13: Commit**

```bash
git add src/utils/teams.ts src/interfaces/index.ts src/hooks/useDartsGame.ts
git commit -m "feat: moteur mode équipes — modèle côtés rétro-compatible

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Classement et enregistrement par côté

**Files:**
- Modify: `src/utils/ranking.ts`
- Modify: `src/utils/gameSummary.ts`

**Interfaces:**
- Consumes: `Team`, GameState `teams`/`sideOf`/`order`; `sidesAsPlayers` from `src/utils/teams.ts`.
- Produces: `liveRanks(state)` renvoie désormais un `Record<sideId, number>` (rang par côté). `summarizeGame` produit une ligne par joueur portant la place/manches de son côté.

- [ ] **Step 1: Réécrire `src/utils/ranking.ts` pour ranker les côtés**

Replace the entire file with:

```ts
import type { GameState } from "@/interfaces";
import { allClosed } from "@/utils/cricket";
import { atcProgress } from "@/utils/aroundClock";

/* The scoring sides of a game: team ids in team mode, player ids in solo. */
function sideIds(state: GameState): string[] {
  return state.teams ? state.teams.map((t) => t.id) : state.players.map((p) => p.id);
}

/* Scores a side for ranking; higher is better in every mode. */
function metric(state: GameState, sideId: string): number {
  if (state.legsTarget > 1) {
    return state.legsWon[sideId] ?? 0;
  }
  const ps = state.states[sideId];
  if (!ps) {
    return 0;
  }
  if (ps.kind === "x01") {
    return -ps.score;
  }
  if (ps.kind === "aroundclock") {
    return atcProgress(ps);
  }
  const closedBonus = allClosed(ps) ? 1000 : 0;
  return state.mode === "cutthroat"
    ? closedBonus - ps.score
    : closedBonus + ps.score;
}

/* Returns each side's live 1-based rank, sharing ties. Keyed by sideId. */
export function liveRanks(state: GameState): Record<string, number> {
  const ordered = [...sideIds(state)].sort(
    (a, b) => metric(state, b) - metric(state, a),
  );
  const ranks: Record<string, number> = {};
  ordered.forEach((sideId, index) => {
    if (index > 0 && metric(state, sideId) === metric(state, ordered[index - 1])) {
      ranks[sideId] = ranks[ordered[index - 1]];
    } else {
      ranks[sideId] = index + 1;
    }
  });
  return ranks;
}
```

Note: consumers that previously did `liveRanks(state)[player.id]` still work in solo (sideId === playerId). Team-mode consumers key by team id.

- [ ] **Step 2: `gameSummary.ts` — place et manches par côté**

In `src/utils/gameSummary.ts`, the current code does `const ranks = liveRanks(state);` then per player reads `ranks[player.id]`, `state.legsWon[player.id]`, and `state.states[player.id]`. In team mode those must be resolved through the player's side. Replace the body of `summarizeGame`'s `.map` so each player resolves its side id first.

Replace this block:

```ts
  const players: GamePlayerSummary[] = state.players.map((player) => {
    const ps = state.states[player.id];
    const stats = state.stats[player.id];
    const darts = stats?.darts ?? 0;
    const isX01 = ps.kind === "x01";
    const points = isX01
      ? state.rules.startScore - ps.score
      : ps.kind === "cricket"
        ? ps.score
        : 0;
    const marks = isX01 ? 0 : (stats?.marks ?? 0);
    const numerator = isX01 ? points : marks;
    const avg3 = darts > 0 ? (numerator / darts) * 3 : 0;

    return {
      profileId: player.profileId ?? null,
      userId: player.friendUserId ?? null,
      name: player.name,
      placement: ranks[player.id] ?? 0,
      legsWon: state.legsWon[player.id] ?? 0,
      darts,
      points,
      marks,
      bestVisit: stats?.bestVisit ?? 0,
      avg3: Math.round(avg3 * 100) / 100,
    };
  });
```

with:

```ts
  const players: GamePlayerSummary[] = state.players.map((player) => {
    const sideId = state.sideOf[player.id] ?? player.id;
    const ps = state.states[sideId];
    const stats = state.stats[player.id];
    const darts = stats?.darts ?? 0;
    const isX01 = ps.kind === "x01";
    const teamPoints = isX01
      ? state.rules.startScore - ps.score
      : ps.kind === "cricket"
        ? ps.score
        : 0;
    const marks = isX01 ? 0 : (stats?.marks ?? 0);
    // Per-player avg uses the player's OWN visits (pointsScored), not the
    // shared team total, so team-mate averages stay individual.
    const avgNumerator = isX01 ? (stats?.pointsScored ?? 0) : marks;
    const avg3 = darts > 0 ? (avgNumerator / darts) * 3 : 0;

    return {
      profileId: player.profileId ?? null,
      userId: player.friendUserId ?? null,
      name: player.name,
      placement: ranks[sideId] ?? 0,
      legsWon: state.legsWon[sideId] ?? 0,
      darts,
      points: isX01 ? (stats?.pointsScored ?? teamPoints) : teamPoints,
      marks,
      bestVisit: stats?.bestVisit ?? 0,
      avg3: Math.round(avg3 * 100) / 100,
    };
  });
```

Note: this also improves solo x01 accuracy by using the player's own `pointsScored` for `points`/`avg3` (in solo, `pointsScored` equals `startScore - score` for a completed game, so no behaviour regression).

- [ ] **Step 3: Vérifier typage + build**

Run: `npx tsc --noEmit && npm run build`
Expected: zéro sortie ; « Compiled successfully ».

- [ ] **Step 4: Commit**

```bash
git add src/utils/ranking.ts src/utils/gameSummary.ts
git commit -m "feat: classement et enregistrement par côté (équipes)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Config — mode Équipes (SetupScreen)

**Files:**
- Modify: `src/components/screens/SetupScreen.tsx`
- Modify: `src/components/screens/SetupScreen.module.css`

**Interfaces:**
- Consumes: `Team` from `@/interfaces`; `teamLabel`, `teamColor` from `@/utils/teams`; existing `useGameInvites`, `usePlayers`, `useSocial`.
- Produces: launches a game with `config.teams` set when in team mode.

Context: `SetupScreen` currently keeps a flat `players: Player[]` state and builds `config = { mode, rules, players, legsTarget }` in `launch`. It already wires `useGameInvites` (`invites`), profiles (`profiles.players`), and friends (`social.friends`) with chips that call `addProfile` / `addFriendSlot`, plus guest slots. Team mode adds a parallel `teams: Team[]` state and a presentation branch. The invite flow (per `friendUserId`) is unchanged — a friend added to a team slot still calls `invites.invite`.

- [ ] **Step 1: Imports et constantes**

Add to the imports in `SetupScreen.tsx`:

```ts
import type { Team } from "@/interfaces";
import { teamColor, teamLabel } from "@/utils/teams";
```

Add near the other module constants (after `LEGS_OPTIONS`):

```ts
const TEAM_PRESETS: { label: string; sizes: number[] }[] = [
  { label: "2v2", sizes: [2, 2] },
  { label: "3v3", sizes: [3, 3] },
  { label: "2v2v2", sizes: [2, 2, 2] },
];
const MAX_TEAMS = 3;
```

- [ ] **Step 2: État mode équipes**

After the existing `const [legsTarget, setLegsTarget] = useState(1);` line, add:

```ts
  const [isTeamMode, setIsTeamMode] = useState(false);
  const [teams, setTeams] = useState<Team[]>([]);
```

- [ ] **Step 3: Helpers de gestion des équipes**

Add these helper functions inside the component, after the existing `removePlayer` definition. They build presets, fill/clear slots, rename and add/remove teams. Each team slot holds a `Player` (guest, profile, or friend), mirroring the solo slot shape.

```ts
  // Team members are kept in a flat players list; a team references them by id.
  // A preset resets the roster to fresh empty slots.
  const applyPreset = (sizes: number[]) => {
    setIsTeamMode(true);
    const built: Team[] = [];
    const flatPlayers: Player[] = [];
    sizes.forEach((count, teamIndex) => {
      const team: Team = {
        id: crypto.randomUUID(),
        name: teamLabel(teamIndex),
        color: teamColor(teamIndex),
        playerIds: [],
      };
      for (let i = 0; i < count; i += 1) {
        const p: Player = { id: crypto.randomUUID(), name: "" };
        flatPlayers.push(p);
        team.playerIds.push(p.id);
      }
      built.push(team);
    });
    setPlayers(flatPlayers);
    setTeams(built);
  };

  // Toggling to team mode keeps the players already entered: they go into
  // team A, with an empty team B to fill.
  const enableTeamMode = () => {
    setIsTeamMode(true);
    if (teams.length > 0) {
      return;
    }
    const teamA: Team = {
      id: crypto.randomUUID(),
      name: teamLabel(0),
      color: teamColor(0),
      playerIds: players.map((p) => p.id),
    };
    const teamB: Team = {
      id: crypto.randomUUID(),
      name: teamLabel(1),
      color: teamColor(1),
      playerIds: [],
    };
    if (players.length < MAX_PLAYERS) {
      const p: Player = { id: crypto.randomUUID(), name: "" };
      teamB.playerIds.push(p.id);
      setPlayers((ps) => [...ps, p]);
    }
    setTeams([teamA, teamB]);
  };

  const disableTeamMode = () => {
    setIsTeamMode(false);
  };

  const renameTeam = (teamId: string, name: string) =>
    setTeams((list) =>
      list.map((t) => (t.id === teamId ? { ...t, name } : t)),
    );

  const addEmptyTeam = () =>
    setTeams((list) => {
      if (list.length >= MAX_TEAMS) {
        return list;
      }
      const p: Player = { id: crypto.randomUUID(), name: "" };
      setPlayers((ps) => [...ps, p]);
      return [
        ...list,
        {
          id: crypto.randomUUID(),
          name: teamLabel(list.length),
          color: teamColor(list.length),
          playerIds: [p.id],
        },
      ];
    });

  const removeTeam = (teamId: string) =>
    setTeams((list) => {
      if (list.length <= 2) {
        return list;
      }
      const team = list.find((t) => t.id === teamId);
      if (team) {
        for (const pid of team.playerIds) {
          const member = players.find((p) => p.id === pid);
          if (member?.friendUserId && member.friendUserId !== auth.user?.id) {
            void invites.cancelForGuest(member.friendUserId);
          }
        }
        setPlayers((ps) => ps.filter((p) => !team.playerIds.includes(p.id)));
      }
      return list.filter((t) => t.id !== teamId);
    });

  const addTeamSlot = (teamId: string) =>
    setTeams((list) =>
      list.map((t) => {
        if (t.id !== teamId) {
          return t;
        }
        const totalPlayers = list.reduce((n, x) => n + x.playerIds.length, 0);
        if (totalPlayers >= MAX_PLAYERS) {
          return t;
        }
        const p: Player = { id: crypto.randomUUID(), name: "" };
        setPlayers((ps) => [...ps, p]);
        return { ...t, playerIds: [...t.playerIds, p.id] };
      }),
    );

  const removeTeamSlot = (teamId: string, playerId: string) => {
    const member = players.find((p) => p.id === playerId);
    if (member?.friendUserId && member.friendUserId !== auth.user?.id) {
      void invites.cancelForGuest(member.friendUserId);
    }
    setTeams((list) =>
      list.map((t) =>
        t.id === teamId
          ? { ...t, playerIds: t.playerIds.filter((id) => id !== playerId) }
          : t,
      ),
    );
    setPlayers((ps) => ps.filter((p) => p.id !== playerId));
  };

  // Fills the first empty slot of a team with a guest/profile/friend player.
  const fillTeamSlot = async (
    teamId: string,
    filled: Partial<Player> & { name: string },
  ) => {
    const team = teams.find((t) => t.id === teamId);
    if (!team) {
      return;
    }
    const emptyId = team.playerIds.find((pid) => {
      const p = players.find((x) => x.id === pid);
      return p && !p.name.trim() && !p.profileId && !p.friendUserId;
    });
    const targetId = emptyId ?? null;
    if (targetId) {
      setPlayers((ps) =>
        ps.map((p) => (p.id === targetId ? { ...p, ...filled } : p)),
      );
    } else {
      const totalPlayers = players.length;
      if (totalPlayers >= MAX_PLAYERS) {
        return;
      }
      const p: Player = { id: crypto.randomUUID(), name: filled.name, ...filled };
      setPlayers((ps) => [...ps, p]);
      setTeams((list) =>
        list.map((t) =>
          t.id === teamId ? { ...t, playerIds: [...t.playerIds, p.id] } : t,
        ),
      );
    }
    if (filled.friendUserId && filled.friendUserId !== auth.user?.id) {
      const error = await invites.invite(filled.friendUserId, mode);
      if (error) {
        setInviteMsg(error);
      }
    }
  };

  const renameMember = (playerId: string, name: string) =>
    setPlayers((list) =>
      list.map((p) => (p.id === playerId ? { ...p, name } : p)),
    );
```

- [ ] **Step 4: Gating du lancement en mode équipe**

Replace the existing `launch` function so it passes `teams` when in team mode and names members. Replace it with:

```ts
  const launch = () => {
    const named = players.map((player, index) => ({
      id: player.id,
      name: player.name.trim() || `Joueur ${index + 1}`,
      profileId: player.profileId,
      friendUserId: player.friendUserId,
    }));
    rememberNames(named.map((player) => player.name));
    const nameById = new Map(named.map((p) => [p.id, p]));
    const cleanTeams = isTeamMode
      ? teams.map((t) => ({
          ...t,
          playerIds: t.playerIds.filter((id) => nameById.has(id)),
        }))
      : undefined;
    game.startGame({
      mode,
      rules,
      players: named,
      legsTarget,
      teams: cleanTeams,
    });
  };

  const emptyTeam = isTeamMode && teams.some((t) => t.playerIds.length === 0);
  const launchDisabled = invites.hasPending || emptyTeam;
```

(If the current file already has an `invites.hasPending`-based disabled launch button from the invites feature, this `launchDisabled` supersedes it — use `launchDisabled` in the button below.)

- [ ] **Step 5: Toggle Individuel / Équipes (rendu)**

At the top of the players `<section className={styles.block}>`, just after the opening `<div className={styles.blockHead}>…</div>`, insert the mode toggle:

```tsx
        <div className={styles.modeToggle}>
          <button
            type="button"
            className={styles.modeToggleBtn}
            data-on={!isTeamMode ? "true" : "false"}
            onClick={disableTeamMode}
          >
            Individuel
          </button>
          <button
            type="button"
            className={styles.modeToggleBtn}
            data-on={isTeamMode ? "true" : "false"}
            onClick={enableTeamMode}
          >
            Équipes
          </button>
        </div>
```

- [ ] **Step 6: Rendu conditionnel — cartes d'équipe**

Wrap the existing solo players UI (the `<div className={styles.players}>…</div>`, the `+ Ajouter un invité` button, and the two `profilesPick` blocks for profiles and friends) in a `{!isTeamMode && ( … )}` guard. Then, immediately after, add the team-mode block:

```tsx
        {isTeamMode && (
          <div className={styles.teamsArea}>
            <div className={styles.presets}>
              <span className={styles.presetLabel}>Rapide</span>
              {TEAM_PRESETS.map((preset) => (
                <button
                  key={preset.label}
                  type="button"
                  className={styles.presetBtn}
                  onClick={() => applyPreset(preset.sizes)}
                >
                  {preset.label}
                </button>
              ))}
              <button
                type="button"
                className={styles.presetBtn}
                disabled={teams.length >= MAX_TEAMS}
                onClick={addEmptyTeam}
                aria-label="Ajouter une équipe"
              >
                +
              </button>
            </div>

            <div className={styles.teamCards}>
              {teams.map((team, teamIndex) => (
                <div
                  key={team.id}
                  className={styles.teamCard}
                  data-color={team.color}
                >
                  <div className={styles.teamCardHead}>
                    <input
                      className={styles.teamName}
                      value={team.name}
                      maxLength={16}
                      onChange={(e) => renameTeam(team.id, e.target.value)}
                      aria-label={`Nom de l'équipe ${teamIndex + 1}`}
                    />
                    {teams.length > 2 && (
                      <button
                        type="button"
                        className={styles.teamRemove}
                        onClick={() => removeTeam(team.id)}
                        aria-label="Retirer l'équipe"
                      >
                        −
                      </button>
                    )}
                  </div>

                  <div className={styles.teamMembers}>
                    {team.playerIds.map((pid) => {
                      const member = players.find((p) => p.id === pid);
                      if (!member) {
                        return null;
                      }
                      const linked = !!(member.profileId || member.friendUserId);
                      const pending =
                        member.friendUserId &&
                        member.friendUserId !== auth.user?.id &&
                        invites.invites[member.friendUserId]?.status !== "accepted";
                      return (
                        <div key={pid} className={styles.teamMemberRow}>
                          <input
                            className={styles.input}
                            value={member.name}
                            placeholder="Joueur"
                            maxLength={14}
                            readOnly={linked}
                            onChange={(e) => renameMember(pid, e.target.value)}
                          />
                          {member.friendUserId &&
                            member.friendUserId !== auth.user?.id && (
                              <span
                                className={styles.inviteStatus}
                                data-status={pending ? "pending" : "accepted"}
                              >
                                {pending ? "en attente…" : "✓"}
                              </span>
                            )}
                          <button
                            type="button"
                            className={styles.remove}
                            aria-label="Retirer le joueur"
                            onClick={() => removeTeamSlot(team.id, pid)}
                          >
                            −
                          </button>
                        </div>
                      );
                    })}
                    <button
                      type="button"
                      className={styles.teamAddSlot}
                      onClick={() => addTeamSlot(team.id)}
                      disabled={
                        players.length >= MAX_PLAYERS
                      }
                    >
                      + joueur
                    </button>
                  </div>

                  {auth.user && (
                    <div className={styles.teamPickers}>
                      {profiles.players.map((profile) => (
                        <button
                          key={profile.id}
                          type="button"
                          className={styles.profileChip}
                          disabled={players.some((p) => p.profileId === profile.id)}
                          onClick={() =>
                            fillTeamSlot(team.id, {
                              name: profile.name,
                              profileId: profile.id,
                            })
                          }
                        >
                          <IconStar /> {profile.name}
                        </button>
                      ))}
                      {social.friends.map((friend) => (
                        <button
                          key={friend.friendshipId}
                          type="button"
                          className={styles.friendChip}
                          disabled={players.some(
                            (p) => p.friendUserId === friend.userId,
                          )}
                          onClick={() =>
                            fillTeamSlot(team.id, {
                              name: `@${friend.username}`,
                              friendUserId: friend.userId,
                            })
                          }
                        >
                          <IconUser /> @{friend.username}
                        </button>
                      ))}
                    </div>
                  )}
                </div>
              ))}
            </div>
          </div>
        )}
```

- [ ] **Step 7: Bouton lancer**

Replace the launch button markup so it uses `launchDisabled` and an explicit label:

```tsx
      <button
        type="button"
        className={styles.launch}
        disabled={launchDisabled}
        onClick={launch}
      >
        {invites.hasPending
          ? "En attente d'acceptation…"
          : emptyTeam
            ? "Complète chaque équipe"
            : "Lancer la partie"}
      </button>
```

- [ ] **Step 8: Styles**

Append to `src/components/screens/SetupScreen.module.css`:

```css
.modeToggle {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 6px;
  padding: 5px;
  margin-bottom: 12px;
  border-radius: 13px;
  border: 1px solid var(--line);
  background: var(--bg-deep);
}

.modeToggleBtn {
  padding: 10px;
  border: none;
  border-radius: 9px;
  background: transparent;
  color: var(--chalk-dim);
  font-size: 0.95rem;
  font-weight: 600;
  transition: all 0.16s ease;
}

.modeToggleBtn[data-on="true"] {
  background: linear-gradient(180deg, var(--surface-2), var(--surface));
  color: var(--chalk);
  box-shadow: 0 2px 10px -4px rgba(0, 0, 0, 0.6);
}

.teamsArea {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.presets {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.presetLabel {
  font-size: 0.66rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--chalk-faint);
}

.presetBtn {
  padding: 9px 16px;
  border: 1px solid var(--line-strong);
  border-radius: 10px;
  background: var(--surface);
  color: var(--chalk);
  font-family: var(--font-display), serif;
  font-size: 1.1rem;
  letter-spacing: 0.04em;
  transition: all 0.14s ease;
}

.presetBtn:hover:not(:disabled) {
  border-color: var(--gold);
  color: var(--gold-bright);
}

.presetBtn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.teamCards {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.teamCard {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 12px;
  border-radius: var(--radius);
  border: 1px solid var(--line);
  border-top: 3px solid var(--team-accent, var(--gold));
  background: linear-gradient(180deg, var(--surface), var(--bg-deep));
}

.teamCard[data-color="teamA"] { --team-accent: var(--gold); }
.teamCard[data-color="teamB"] { --team-accent: #4c9be8; }
.teamCard[data-color="teamC"] { --team-accent: #56c583; }

.teamCardHead {
  display: flex;
  align-items: center;
  gap: 8px;
}

.teamName {
  flex: 1;
  min-width: 0;
  padding: 6px 8px;
  border: 1px solid transparent;
  border-radius: 8px;
  background: var(--bg-deep);
  color: var(--chalk);
  font-family: var(--font-display), serif;
  font-size: 1.15rem;
  letter-spacing: 0.03em;
}

.teamName:focus {
  border-color: var(--line-strong);
  outline: none;
}

.teamRemove {
  flex-shrink: 0;
  width: 30px;
  height: 30px;
  border-radius: 8px;
  border: 1px solid var(--line);
  background: var(--bg-deep);
  color: var(--chalk-dim);
  font-size: 1.2rem;
  line-height: 1;
}

.teamMembers {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.teamMemberRow {
  display: flex;
  align-items: center;
  gap: 6px;
}

.teamAddSlot {
  padding: 8px;
  border: 1px dashed var(--line-strong);
  border-radius: 8px;
  background: transparent;
  color: var(--chalk-dim);
  font-size: 0.85rem;
  font-weight: 600;
}

.teamAddSlot:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.teamPickers {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  padding-top: 8px;
  border-top: 1px solid var(--line);
}
```

- [ ] **Step 9: Vérifier typage + build**

Run: `npx tsc --noEmit && npm run build`
Expected: zéro sortie ; « Compiled successfully ».

- [ ] **Step 10: Commit**

```bash
git add src/components/screens/SetupScreen.tsx src/components/screens/SetupScreen.module.css
git commit -m "feat: config mode équipes — toggle, presets, cartes éditables

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Scoreboard en jeu — cartes d'équipe

**Files:**
- Create: `src/components/ui/TeamScoreCard.tsx`
- Create: `src/components/ui/TeamScoreCard.module.css`
- Modify: `src/components/screens/GameScreen.tsx`

**Interfaces:**
- Consumes: `Team`, `Player`, `PlayerGameState`, `PlayerStats` from `@/interfaces`; `liveRanks` from `@/utils/ranking`; `atcProgress`, `ATC_SEQUENCE` from `@/utils/aroundClock`; `CRICKET_NUMBERS`, `allClosed` from `@/utils/cricket`.
- Produces: `TeamScoreCard` component consumed by `GameScreen` in team mode.

- [ ] **Step 1: Créer `src/components/ui/TeamScoreCard.tsx`**

```tsx
"use client";

import type {
  Player,
  PlayerGameState,
  PlayerStats,
  Team,
} from "@/interfaces";
import { ATC_SEQUENCE, atcProgress } from "@/utils/aroundClock";
import { CRICKET_NUMBERS } from "@/utils/cricket";
import styles from "./TeamScoreCard.module.css";

interface TeamScoreCardProps {
  team: Team;
  state: PlayerGameState;
  members: Player[];
  stats: Record<string, PlayerStats>;
  legsWon: number;
  showLegs: boolean;
  rank: number;
  showRank: boolean;
  isCurrentTeam: boolean;
  currentPlayerId: string | null;
  isWinner: boolean;
}

const RANK_LABEL = ["", "1ᵉʳ", "2ᵉ", "3ᵉ"];

/* Team scoreboard card: shared score at the centre, member rows below. */
export function TeamScoreCard({
  team,
  state,
  members,
  stats,
  legsWon,
  showLegs,
  rank,
  showRank,
  isCurrentTeam,
  currentPlayerId,
  isWinner,
}: TeamScoreCardProps) {
  return (
    <article
      className={styles.card}
      data-color={team.color}
      data-current={isCurrentTeam ? "true" : "false"}
      data-winner={isWinner ? "true" : "false"}
    >
      <header className={styles.head}>
        {showRank && !isWinner && (
          <span className={styles.rank} data-lead={rank === 1 ? "true" : "false"}>
            {RANK_LABEL[rank] ?? `${rank}e`}
          </span>
        )}
        <span className={styles.name}>{team.name}</span>
        {showLegs && <span className={styles.legs}>{legsWon}</span>}
        {isWinner && <span className={styles.badge}>Gagné</span>}
      </header>

      {state.kind === "x01" ? (
        <div className={styles.score}>
          {state.score}
          {!state.opened && <span className={styles.locked}>fermé</span>}
        </div>
      ) : state.kind === "aroundclock" ? (
        <div className={styles.score}>
          {state.target === 0
            ? "✓"
            : state.target === 25
              ? "Bull"
              : state.target}
          <span className={styles.atcFraction}>
            {atcProgress(state)}/{ATC_SEQUENCE.length}
          </span>
        </div>
      ) : (
        <div className={styles.cricket}>
          {CRICKET_NUMBERS.map((number) => (
            <div key={number} className={styles.markRow}>
              <span className={styles.markNum}>
                {number === 25 ? "B" : number}
              </span>
              <span className={styles.pips} data-closed={state.marks[number] >= 3 ? "true" : "false"}>
                <span className={styles.pip} data-on={state.marks[number] >= 1 ? "true" : "false"} />
                <span className={styles.pip} data-on={state.marks[number] >= 2 ? "true" : "false"} />
                <span className={styles.pip} data-on={state.marks[number] >= 3 ? "true" : "false"} />
              </span>
            </div>
          ))}
          <div className={styles.cricketScore}>
            <span className={styles.cricketScoreLabel}>Points</span>
            <span className={styles.cricketScoreValue}>{state.score}</span>
          </div>
        </div>
      )}

      <div className={styles.members}>
        {members.map((member) => {
          const ms = stats[member.id];
          return (
            <div
              key={member.id}
              className={styles.memberRow}
              data-throwing={member.id === currentPlayerId ? "true" : "false"}
            >
              <span className={styles.memberName}>{member.name}</span>
              <span className={styles.memberStat}>
                {ms && ms.darts > 0 ? `${ms.lastVisit}` : "—"}
              </span>
            </div>
          );
        })}
      </div>
    </article>
  );
}
```

- [ ] **Step 2: Créer `src/components/ui/TeamScoreCard.module.css`**

```css
.card {
  position: relative;
  flex: 1;
  min-width: 0;
  padding: 14px;
  border-radius: var(--radius);
  border: 1px solid var(--line);
  border-top: 3px solid var(--team-accent, var(--gold));
  background: linear-gradient(180deg, var(--surface), var(--bg-deep));
  transition: border-color 0.18s ease, transform 0.18s ease, box-shadow 0.18s ease;
}

.card[data-color="teamA"] { --team-accent: var(--gold); }
.card[data-color="teamB"] { --team-accent: #4c9be8; }
.card[data-color="teamC"] { --team-accent: #56c583; }

.card[data-current="true"] {
  box-shadow: 0 0 0 1px var(--team-accent), 0 10px 28px -16px rgba(0, 0, 0, 0.6);
  transform: translateY(-3px);
}

@keyframes teamWinnerPulse {
  0%, 100% { box-shadow: 0 0 0 1px var(--green), 0 0 0 0 rgba(42, 161, 94, 0); }
  50% { box-shadow: 0 0 0 1px var(--green), 0 0 22px 6px rgba(42, 161, 94, 0.35); }
}

.card[data-winner="true"] {
  border-color: var(--green);
  animation: teamWinnerPulse 1.1s ease 3;
}

.head {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}

.rank {
  flex-shrink: 0;
  min-width: 22px;
  height: 22px;
  padding: 0 5px;
  display: inline-grid;
  place-items: center;
  border-radius: 7px;
  background: var(--surface-2);
  border: 1px solid var(--line);
  color: var(--chalk-dim);
  font-size: 0.72rem;
  font-weight: 700;
}

.rank[data-lead="true"] {
  background: linear-gradient(180deg, var(--gold), #b48f38);
  border-color: var(--gold);
  color: var(--ink);
}

.name {
  flex: 1;
  min-width: 0;
  font-size: 1rem;
  font-weight: 600;
  color: var(--chalk);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.legs {
  flex-shrink: 0;
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--team-accent, var(--gold-bright));
}

.badge {
  flex-shrink: 0;
  font-size: 0.64rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--green);
  border: 1px solid var(--green);
  border-radius: 999px;
  padding: 2px 8px;
}

.score {
  font-family: var(--font-display), serif;
  font-size: clamp(2.6rem, 12vw, 3.8rem);
  line-height: 0.9;
  color: var(--chalk);
  display: flex;
  align-items: baseline;
  gap: 8px;
}

.atcFraction {
  font-family: var(--font-body), sans-serif;
  font-size: 0.8rem;
  color: var(--chalk-dim);
}

.locked {
  font-family: var(--font-body), sans-serif;
  font-size: 0.62rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--red);
}

.cricket {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.markRow {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.markNum {
  font-family: var(--font-display), serif;
  font-size: 1.05rem;
  color: var(--chalk-dim);
  width: 22px;
}

.pips {
  display: flex;
  gap: 4px;
}

.pip {
  width: 11px;
  height: 11px;
  border-radius: 50%;
  border: 1.5px solid var(--chalk-faint);
}

.pip[data-on="true"] {
  background: var(--gold);
  border-color: var(--gold);
}

.pips[data-closed="true"] .pip {
  background: var(--green);
  border-color: var(--green);
}

.cricketScore {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 8px;
  margin-top: 4px;
  border-top: 1px solid var(--line);
}

.cricketScoreLabel {
  font-size: 0.64rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--chalk-faint);
}

.cricketScoreValue {
  font-family: var(--font-display), serif;
  font-size: 1.7rem;
  color: var(--chalk);
}

.members {
  display: flex;
  flex-direction: column;
  gap: 3px;
  margin-top: 10px;
  padding-top: 8px;
  border-top: 1px solid var(--line);
}

.memberRow {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding: 4px 6px;
  border-radius: 7px;
  transition: background 0.16s ease;
}

.memberRow[data-throwing="true"] {
  background: rgba(201, 164, 74, 0.12);
  box-shadow: inset 0 0 0 1px var(--gold);
}

.memberName {
  min-width: 0;
  font-size: 0.86rem;
  color: var(--chalk);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.memberRow[data-throwing="true"] .memberName {
  color: var(--gold-bright);
  font-weight: 600;
}

.memberStat {
  flex-shrink: 0;
  font-family: var(--font-display), serif;
  font-size: 1rem;
  color: var(--chalk-dim);
}
```

- [ ] **Step 3: Brancher `GameScreen` sur les cartes d'équipe**

In `src/components/screens/GameScreen.tsx`, add the import near the other UI imports:

```tsx
import { TeamScoreCard } from "@/components/ui/TeamScoreCard";
```

Locate the scoreboard block that maps `state.players` into `<PlayerScoreCard>` (the `<div className={styles.board} …>`). Replace that whole `<div className={styles.board}>…</div>` with a branch that renders team cards when `state.teams` is set:

```tsx
      <div
        className={styles.board}
        data-many={
          (state.teams ? state.teams.length : state.players.length) > 2
            ? "true"
            : "false"
        }
      >
        {state.teams
          ? state.teams.map((team) => (
              <TeamScoreCard
                key={team.id}
                team={team}
                state={state.states[team.id]}
                members={team.playerIds
                  .map((pid) => state.players.find((p) => p.id === pid))
                  .filter((p): p is NonNullable<typeof p> => p != null)}
                stats={state.stats}
                legsWon={state.legsWon[team.id] ?? 0}
                showLegs={state.legsTarget > 1}
                rank={ranks[team.id]}
                showRank={state.teams.length > 1}
                isCurrentTeam={
                  currentPlayer != null &&
                  state.sideOf[currentPlayer.id] === team.id &&
                  !state.winnerId
                }
                currentPlayerId={currentPlayer?.id ?? null}
                isWinner={state.winnerId === team.id}
              />
            ))
          : state.players.map((player, index) => (
              <PlayerScoreCard
                key={player.id}
                player={player}
                state={state.states[player.id]}
                stats={state.stats[player.id]}
                legsWon={state.legsWon[player.id] ?? 0}
                showLegs={state.legsTarget > 1}
                startScore={state.rules.startScore}
                rank={ranks[player.id]}
                showRank={state.players.length > 1}
                isCurrent={index === state.currentIndex && !state.winnerId}
                isWinner={state.winnerId === player.id}
              />
            ))}
      </div>
```

Note: `ranks` (from `liveRanks(state)`) is already computed in `GameScreen`; in team mode it is keyed by team id, which matches `ranks[team.id]`. The turn header already shows `currentPlayer.name`; optionally the team name can be added but is not required.

- [ ] **Step 4: Vérifier typage + build**

Run: `npx tsc --noEmit && npm run build`
Expected: zéro sortie ; « Compiled successfully ».

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/TeamScoreCard.tsx src/components/ui/TeamScoreCard.module.css src/components/screens/GameScreen.tsx
git commit -m "feat: scoreboard équipes — cartes d'équipe + lanceur courant

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: Écran de résultat — podium par équipe

**Files:**
- Modify: `src/components/screens/ResultScreen.tsx`
- Modify: `src/components/screens/ResultScreen.module.css`

**Interfaces:**
- Consumes: `Team`, `Player` from `@/interfaces`; existing `computeAvg`, `PLACE_LABELS`, `MEDALS` in ResultScreen; `atcProgress` from `@/utils/aroundClock`.

Context: `ResultScreen` currently ranks players and renders a `PlayerResultCard` each. In team mode we rank teams and render a team card containing its members' per-player stats. The `rankPlayers` helper sorts players; we add a `rankTeams` helper and branch the render.

- [ ] **Step 1: Ajouter le classement des équipes**

In `src/components/screens/ResultScreen.tsx`, add a `rankTeams` helper after the existing `rankPlayers` function:

```tsx
/* Orders teams from best to worst for the final podium. */
function rankTeams(game: DartsGame): Team[] {
  const { state } = game;
  const teams = state.teams ?? [];
  return [...teams].sort((a, b) => {
    if (state.legsTarget > 1) {
      return (state.legsWon[b.id] ?? 0) - (state.legsWon[a.id] ?? 0);
    }
    const sa = state.states[a.id];
    const sb = state.states[b.id];
    if (sa.kind === "x01" && sb.kind === "x01") {
      return sa.score - sb.score;
    }
    if (sa.kind === "aroundclock" && sb.kind === "aroundclock") {
      return atcProgress(sb) - atcProgress(sa);
    }
    if (sa.kind === "cricket" && sb.kind === "cricket") {
      return state.mode === "cutthroat" ? sa.score - sb.score : sb.score - sa.score;
    }
    return 0;
  });
}
```

Add the `Team` type to the interfaces import and `atcProgress` if not already imported (the file already imports `atcProgress`, `ATC_SEQUENCE` from `@/utils/aroundClock` and `Player` from `@/interfaces`; extend the interfaces import to include `Team`).

- [ ] **Step 2: Composant carte d'équipe résultat**

Add a `TeamResultCard` component before the main `ResultScreen` export. It shows the team medal + name, then each member's per-player stats (reusing the same stat computations as `PlayerResultCard`).

```tsx
interface TeamResultCardProps {
  team: Team;
  game: DartsGame;
  rank: number;
}

function TeamResultCard({ team, game, rank }: TeamResultCardProps) {
  const { state } = game;
  const medal = MEDALS[rank - 1];
  const isWinner = rank === 1;
  const members = team.playerIds
    .map((pid) => state.players.find((p) => p.id === pid))
    .filter((p): p is Player => p != null);

  const summary =
    state.legsTarget > 1
      ? `${state.legsWon[team.id] ?? 0} manche${(state.legsWon[team.id] ?? 0) > 1 ? "s" : ""}`
      : (() => {
          const ps = state.states[team.id];
          if (ps.kind === "x01") return ps.score === 0 ? "Checkout !" : `${ps.score} restants`;
          if (ps.kind === "aroundclock") return ps.target === 0 ? "Tour complet !" : `Cible ${ps.target}`;
          return `${ps.score} pts`;
        })();

  return (
    <div
      className={styles.playerCard}
      data-winner={isWinner ? "true" : "false"}
      style={{ animationDelay: `${(rank - 1) * 0.08}s` }}
    >
      <div className={styles.playerCardHead}>
        <span className={styles.playerMedal} data-medal={medal ?? "none"}>
          {PLACE_LABELS[rank - 1] ?? `${rank}e`}
        </span>
        <span className={styles.playerCardName}>{team.name}</span>
        <span className={styles.playerSummary}>{summary}</span>
      </div>

      <div className={styles.teamMembers}>
        {members.map((member) => {
          const pStats = state.totalStats[member.id];
          const avg = computeAvg(game, member);
          return (
            <div key={member.id} className={styles.teamMemberStat}>
              <span className={styles.teamMemberName}>{member.name}</span>
              <span className={styles.teamMemberVals}>
                {avg.toFixed(1)} moy · {pStats?.bestVisit || "—"} best · {pStats?.darts ?? 0} fléch.
              </span>
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Brancher le rendu du podium**

In the `ResultScreen` component, compute the team ranking and branch. After the existing `const ranking = rankPlayers(game);` line add:

```tsx
  const teamRanking = state.teams ? rankTeams(game) : null;
  const winnerName = teamRanking ? teamRanking[0]?.name : ranking[0]?.name;
```

Replace the usage of `winner?.name` in the header with `winnerName`. Then in the breakdown `<div className={styles.playerCards}>`, branch:

```tsx
        <div className={styles.playerCards}>
          {teamRanking
            ? teamRanking.map((team, index) => (
                <TeamResultCard
                  key={team.id}
                  team={team}
                  game={game}
                  rank={index + 1}
                />
              ))
            : ranking.map((player, index) => (
                <PlayerResultCard
                  key={player.id}
                  player={player}
                  game={game}
                  rank={index + 1}
                />
              ))}
        </div>
```

(If the header currently derives `winner` via `const winner = ranking[0];`, keep it for solo but use `winnerName` for the displayed name so team mode shows the team name.)

- [ ] **Step 4: Styles des membres d'équipe**

Append to `src/components/screens/ResultScreen.module.css`:

```css
.teamMembers {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding-top: 10px;
  border-top: 1px solid var(--line);
}

.teamMemberStat {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 10px;
}

.teamMemberName {
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--chalk);
}

.teamMemberVals {
  font-size: 0.76rem;
  color: var(--chalk-dim);
}
```

- [ ] **Step 5: Vérifier typage + build**

Run: `npx tsc --noEmit && npm run build`
Expected: zéro sortie ; « Compiled successfully ».

- [ ] **Step 6: Commit**

```bash
git add src/components/screens/ResultScreen.tsx src/components/screens/ResultScreen.module.css
git commit -m "feat: écran résultat — podium par équipe + stats membres

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Vérification manuelle finale

Après les 5 tâches (`npm run build` OK) :

1. **Solo non-régression** : lancer un 501 solo à 2 joueurs, jouer quelques tours, bust, victoire, écran résultat, rejouer. Doit être identique à avant.
2. **2v2 X01** : mode Équipes → preset 2v2 → remplir 4 invités → lancer. Vérifier l'alternance des lanceurs (A1, B1, A2, B2), le score partagé qui descend, un bust qui annule la volée, la victoire d'équipe, le podium par équipe avec stats par membre.
3. **2v2v2** : preset 2v2v2 → 6 invités → vérifier 3 cartes d'équipe et la rotation.
4. **Cricket équipe** : 2v2 Cricket → marques partagées par équipe, fermeture, victoire.
5. **Comptes liés** : mettre un profil ⭐ et un ami 👤 (avec invitation acceptée) dans une équipe → terminer → vérifier que chacun retrouve la partie dans « Mes stats » avec ses stats perso et la place de l'équipe.
6. **Renommage** : renommer une équipe dans la config → le nom apparaît en jeu et au résultat.

---

## Self-Review (effectuée à l'écriture)

- **Couverture spec** : modèle côtés (Task 1) ✓ ; buildOrder/sideOf/setupSides (Task 1, teams.ts) ✓ ; scoring/victoire/legs par côté (Task 1 reduceRegister/NEXT_TURN/REMOVE_FROM) ✓ ; HYDRATE rétro-compat (Task 1) ✓ ; classement côté (Task 2 ranking) ✓ ; enregistrement par joueur + place équipe (Task 2 gameSummary) ✓ ; toggle+presets+cartes éditables+gating (Task 3) ✓ ; noms éditables (Task 3 renameTeam) ✓ ; comptes/amis/invitations dans slots équipe (Task 3 fillTeamSlot) ✓ ; scoreboard cartes équipe + lanceur courant (Task 4) ✓ ; podium équipe + stats membres (Task 5) ✓.
- **Cohérence des types** : `sideOf`/`order`/`teams` définis en Task 1 et lus identiquement ensuite ; `liveRanks` renvoie un Record keyé par sideId (Task 2) consommé par Task 4/5 via `ranks[team.id]` ; `setupSides`/`sidesAsPlayers` signatures uniques ; `Team { id, name, playerIds, color }` cohérent partout.
- **Non-régression solo** : en solo `teams=null`, `sideOf` identité, `order=players ids` → toutes les clés `states/legsWon/winnerId` restent des playerIds, UI solo (PlayerScoreCard, PlayerResultCard) inchangée.
- **Pas de placeholder** : code complet à chaque étape.
